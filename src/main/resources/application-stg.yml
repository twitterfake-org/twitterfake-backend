spring:
  application:
    name: TWITTERFAKE_STG
  datasource:
    url: jdbc:mysql://localhost:3306/twitterfake?useSSL=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
    show-sql: true